backend:
  name: github
  repo: wanliu530/lenamirror
  branch: main
  auth_scope: repo
  auth_endpoint: https://decap-oauth-proxy.786001020.workers.dev/api/auth

media_folder: "assets/upload"
public_folder: "/assets/upload"

collections:
  - name: journal
    label: "随笔 Journal"
    folder: "content/journal"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{title}}"
    fields:
      - { label: "标题", name: "title", widget: "string" }
      - { label: "发布日期", name: "date", widget: "datetime" }
      - { label: "正文内容（支持图片插入）", name: "body", widget: "markdown" }

  - name: gallery
    label: "Gallery 相册"
    folder: "content/gallery"
    create: true
    slug: "{{albumId}}-{{date}}"
    fields:
      - label: "相册分类"
        name: albumId
        widget: select
        options:
          - { label: "Daily Life", value: "dailylife" }
          - { label: "Portrait", value: "portrait" }
          - { label: "刘咪", value: "liumi" }
      - { label: "照片简介", name: "caption", widget: "text", required: false }
      - { label: "照片文件", name: "photo", widget: "image" }